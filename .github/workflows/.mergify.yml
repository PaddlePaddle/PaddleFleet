pull_request_rules:
    - name: auto add to merge queue
      conditions:
        - check-success=Test
        - check-success=Test-release
        - approved-reviews-by>=1
        - base=develop
        - updated-at<24 hours ago
      actions:
        queue: